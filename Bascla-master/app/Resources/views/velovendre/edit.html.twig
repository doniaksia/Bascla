{% extends 'base2.html.twig' %}

{% block content %}
<h4 class="card-title">
    {% form_theme edit_form 'bootstrap_4_horizontal_layout.html.twig' %}
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">velo </h4>
        </div>
        <h4 class="card-body">


    <center><h1>Velovendre edit</h1></center>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}

    <center><button class="btn btn-primary" type="submit">Edit</button></center>
    {{ form_end(edit_form) }}
<br>
    <ul>

            {{ form_start(delete_form) }}
        <center>    <button class="btn btn-primary" type="submit"  >Supprimer  </button></center>

            {{ form_end(delete_form) }}
        </li>
    </ul>
        </h4>
{% endblock %}
