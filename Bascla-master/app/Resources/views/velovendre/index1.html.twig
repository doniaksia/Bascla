{% extends 'base2.html.twig' %}
{% block header %}
    {{ parent() }}

{% endblock %}

{% block content %}

<!-- pidev DataTable / dataBase Start -->
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <center><h3 >velo a vendre</h3></center>
        </div>
        <div class="card-body">
            <div class="table-responsive ">
                <table class="table header-border table-responsive-sm text-muted">






        <thead>
        <tr>
            <th>Id</th>
            <th>description</th>
            <th>prix</th>
            <th>Type</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>

        {% for velovendre in velovendres %}
        <center>

            <tr>
                 <td><center><a href="{{ path('velovendre_show', { 'id': velovendre.id }) }}">{{ velovendre.id }}</a></center></td>
                   <td><center>{% if velovendre.description %}{{ velovendre.description}}{% endif %}</center></td>

                       <td><center>{% if velovendre.prix %}{{ velovendre.prix }}{% endif %}</center></center></td>
            <td><center>{% if velovendre.type %}{{ velovendre.type }}{% endif %}</center></td>

            <td>
                    <ul>


                        <center> <a href="{{ path('velovendre_edit', { 'id': velovendre.id })  }}"class="btn btn-outline-success">edit</a></center>

                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>

    </table>

</center>

{% endblock %}

